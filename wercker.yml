box: opensuse

build:
  steps:
    - script:
        name: migate
        code: |
          zypper install -y git curl which &> /dev/null
          CI_PROJECT_DIR=$(pwd)
          git clone --branch Tests https://${USERNAME}:${PASSWORD}@dev.azure.com/blackcat0115/${LIBBASE}/_git/${LIBBASE} 
          mkdir -p ${CI_PROJECT_DIR}/content
          cd ${CI_PROJECT_DIR}/content
          bash ${TRAVIS_BUILD_DIR}/${LIBBASE}/Tests/Pipeline/Create.sh ${METHOD} ${REPO} ${BRANCH} ${PACKAGES}
